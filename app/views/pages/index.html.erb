<% if user_signed_in? %>
  <%= react_component 'AuthenticatedApp', current_user: {
    id: current_user.id,
    email: current_user.email
  } %>
<% else %>
  <%= react_component 'UnauthenticatedApp', props: {'logged_in': false} %>
<% end %>
